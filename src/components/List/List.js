import React from 'react';
import styles from './List.scss';

const List = () => (
      <section className={styles.component}>
        <h2>Things to do</h2>
      </section>
);

export default List;
